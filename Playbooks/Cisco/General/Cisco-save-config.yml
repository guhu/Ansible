---
- name: Ping IOS
  hosts: all
  connection: local
  gather_facts: false

  tasks:

    - name: Ping IOS
      ios_command:
        commands:
          - write memory
      register: output

    - debug: var=output.stdout_lines